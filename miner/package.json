{
  "name": "@btc-platform/miner",
  "version": "0.1.0",
  "private": true,
  "description": "Real Bitcoin SHA-256d CPU miner that speaks Stratum V1. Solves work handed out by a pool, finds shares, and submits them back. Hashrate is whatever your CPU genuinely produces — typically tens of MH/s per core via Node crypto. No fake numbers, no simulated rewards.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "20.12.7",
    "tsx": "4.7.2",
    "typescript": "5.4.5"
  }
}
